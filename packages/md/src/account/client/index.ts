/**
 * Client-часть модуля Account: Pinia-store + Vue-форма.
 * Импортируется через `@aid/md/account/client` (apps/client).
 */
export { useAccountStore } from './store.js'
export { default as AccountForm } from './form.vue'

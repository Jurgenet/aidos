# AID root .env (copy to .env to use locally — .env is gitignored)

NODE_ENV=development
LOG_LEVEL=debug
HOST=0.0.0.0
PORT=3000

# MongoDB
MONGO_URI=mongodb://localhost:27017/aid
MONGO_DB_NAME=aid

# Graceful shutdown timeout in ms
SHUTDOWN_TIMEOUT_MS=10000
